import { Button, Card, Input } from "@giveaway/ui"; import { useState } from "react";
export function WatchlistsPage() { const [creator,setCreator]=useState(""); const [items,setItems]=useState(["@aurora_chain","@orbit_rewards"]); return <main className="space-y-4"><div><p className="eyebrow">Monitoring</p><h1>Watchlists</h1></div><div className="search-row"><Input value={creator} onChange={e=>setCreator(e.target.value)} placeholder="@creator"/><Button onClick={()=>{if(creator) setItems([...items,creator]);setCreator("");}}>Add</Button></div><Card><h2>Creator watchlist</h2>{items.map(item=><div className="activity" key={item}><span><strong>{item}</strong><small>Trust changes · New evidence</small></span><button className="back" onClick={()=>setItems(items.filter(value=>value!==item))}>Remove</button></div>)}</Card></main>; }
