import { useAppStore } from "./store";
import { SearchPage } from "./pages/SearchPage"; import { ProfilePage } from "./pages/ProfilePage"; import { SettingsPage } from "./pages/SettingsPage"; import { DashboardPage } from "./pages/DashboardPage"; import { InvestigationPage } from "./pages/InvestigationPage"; import { WatchlistsPage } from "./pages/WatchlistsPage";
export function App() { const { page, setPage } = useAppStore(); const views={dashboard:<DashboardPage/>,search:<SearchPage/>,profile:<ProfilePage/>,investigation:<InvestigationPage/>,watchlists:<WatchlistsPage/>,settings:<SettingsPage/>}; return <div className="shell"><nav><div className="brand">GI<span>•</span></div>{(["dashboard","search","watchlists","settings"] as const).map(item=><button key={item} className={page===item?"active":""} onClick={()=>setPage(item)}>{item}</button>)}</nav>{views[page]}</div>; }
