import type{FastifyPluginAsync}from"fastify";import{queueMetrics}from"../lib/queue-metrics.js";let requests=0;export const incrementRequests=()=>requests++;export const operationsRoutes:FastifyPluginAsync=async app=>{app.get("/live",async()=>({status:"live"}));app.get("/ready",async()=>({status:"ready",dependencies:"degraded-safe"}));app.get("/metrics",async()=>`gi_requests_total ${requests}\ngi_uptime_seconds ${process.uptime()}`);app.get("/admin/monitoring",async()=>{try{return{apiUptime:process.uptime(),queues:await queueMetrics(),workerStatus:"configured",connectorHealth:"credential-dependent"};}catch{return{apiUptime:process.uptime(),queues:[],workerStatus:"degraded",connectorHealth:"credential-dependent"};}});};
