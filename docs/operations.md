# Operations

Use `/live` for process liveness, `/ready` for dependency readiness, `/metrics` for request metrics, and `/admin/monitoring` for queue and worker status. Redis cache failures degrade to direct reads.
