{
  "name": "giveaway-intelligence",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @giveaway/database generate",
    "db:migrate": "pnpm --filter @giveaway/database migrate"
  },
  "devDependencies": { "typescript": "^5.7.3" }
}
