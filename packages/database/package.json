{
  "name": "@giveaway/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "build": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "dependencies": { "@prisma/client": "^6.3.1" },
  "devDependencies": { "@types/node": "^22.13.4", "prisma": "^6.3.1", "typescript": "^5.7.3" }
}
